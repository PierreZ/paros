# Generated by Cargo
# will have compiled files and executables
debug
target

# These are backup files generated by rustfmt
**/*.rs.bk

# Added by cargo

/target

# Nix / direnv
.direnv/

# mdBook build artifacts (`mdbook build`)
/book/output/

# Library crate: lockfile is not committed
/Cargo.lock

# Claude Code local settings
.claude/settings.local.json

# Claude Code agent worktrees (nested checkouts, never project content)
.claude/worktrees/

# paros play: installed npm packages, the vite bundle, and the wasm-bindgen output
# (scripts/build-play.sh produces the last two; src/generated/ is committed).
/web/play/node_modules/
/web/play/dist/
/web/play/src/wasm/

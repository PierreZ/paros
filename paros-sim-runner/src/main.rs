//! Native entry point for running paros deterministic simulations.
//!
//! Stage 1 (#14) wires this up to drive `paros-sim` workloads under moonpool
//! (seed replay, the fork-based explorer, coverage). Stage 0 is an empty stub.

fn main() {
    println!("paros-sim-runner: no simulations wired up yet (Stage 1)");
}
